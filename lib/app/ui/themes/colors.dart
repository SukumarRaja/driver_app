import 'dart:ui';

import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Color.fromRGBO(255, 143, 0, 1);
  static const tertiaryColor = Color.fromRGBO(0, 0, 0, 1);
  static const secondaryColor = Color.fromRGBO(255, 255, 255, 1.0);
  static const grey = Colors.grey;
  static const white = Colors.white;
}
